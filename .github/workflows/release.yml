name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Packages according to .pkgmeta (package-as, externals such as
      # LibEditMode, ignore list, changelog), substitutes TOC keywords like
      # @project-version@ from the git tag, attaches the trimmed zip to a
      # GitHub release for the tag, and uploads to CurseForge (project from
      # X-Curse-Project-ID in the TOC) using the org-level CF_API_KEY secret.
      - name: Package and release
        uses: BigWigsMods/packager@v2
        env:
          GITHUB_OAUTH: ${{ secrets.GITHUB_TOKEN }}
          CF_API_KEY: ${{ secrets.CF_API_KEY }}
